package rst.pdfbox.layout.elements.render;

/**
 * Each element in a document is accompanied by a hint, which gives some notes
 * to the current layout on how to layout the element.
 */
public interface LayoutHint {

}
